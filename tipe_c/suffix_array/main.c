#include <stdlib.h>
#include <stdio.h>
#include <string.h>

#include "suffix_array.h"

int main(int argc, char *argv[]) {
    str test_str = "cabbage";
    show_typemap(test_str, strlen(test_str));
    return 0;
}
